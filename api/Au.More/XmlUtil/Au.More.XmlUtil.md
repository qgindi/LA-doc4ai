# Class `Au.More.XmlUtil`

Loads `System.Xml.Linq.XElement` and `System.Xml.Linq.XDocument` in a safer way.

```csharp
public static class XmlUtil
```

##### Inheritance

`object` → `XmlUtil`

### Methods

`LoadDoc`, `LoadElem`, `LoadElem`, `LoadElemIfExists`