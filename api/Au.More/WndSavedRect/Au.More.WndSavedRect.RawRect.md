# Property `Au.More.WndSavedRect.RawRect`

Window rectangle in normal state (not maximized/minimized), as retrieved by API `GetWindowPlacement`.

```
public RECT RawRect { get; set; }
```

##### Property Value

`Au.Types.RECT`