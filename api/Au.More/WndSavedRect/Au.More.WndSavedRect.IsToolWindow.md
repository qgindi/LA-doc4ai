# Property `Au.More.WndSavedRect.IsToolWindow`

`Au.wnd.IsToolWindow`. If `false`, `Au.More.WndSavedRect.RawRect` may have an offset that depends on work area.

```csharp
public bool IsToolWindow { readonly get; set; }
```

##### Property Value

`bool`