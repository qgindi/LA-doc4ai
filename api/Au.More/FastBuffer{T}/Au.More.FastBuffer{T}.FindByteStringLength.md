# Method `Au.More.FastBuffer<T>.FindByteStringLength`

Finds length of `'\0'`-terminated `byte` string in buffer. Returns `Au.More.FastBuffer<T>.n` if there is no `'\0'` character.

```
public int FindByteStringLength()
```

##### Returns

`int`