# Operator `Au.More.FastBuffer<T>.implicit operator`

Returns memory buffer pointer (`Au.More.FastBuffer<T>.p`).

```
public static implicit operator T*(in FastBuffer<T> b)
```

##### Parameters

- *b*  (`Au.More.FastBuffer<T>`)

##### Returns

`T*`