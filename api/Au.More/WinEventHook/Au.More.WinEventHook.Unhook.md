# Method `Au.More.WinEventHook.Unhook`

Removes all hooks.

```
public void Unhook()
```

#### Remarks

Does nothing if already removed or wasn't set. Must be called from the same thread that sets the hook.