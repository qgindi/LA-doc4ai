# Method `Au.More.WinEventHook.Remove`

Removes a hook added by `Au.More.WinEventHook.Add`.

```
public void Remove(int addedId)
```

##### Parameters

- *addedId*  (`int`):
    A return value of `Au.More.WinEventHook.Add`.

##### Exceptions

- `ArgumentException`