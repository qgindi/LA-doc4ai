# Method `Au.More.Math2.NintToPOINT`

Converts `nint` containing x and y coordinates to `Au.Types.POINT`.

```
public static POINT NintToPOINT(nint xy)
```

##### Parameters

- *xy*  (`nint`)

##### Returns

`Au.Types.POINT`