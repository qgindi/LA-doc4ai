# Method `Au.More.ExplorerFolder.GetFolderPath`

Gets folder path.
For non-filesystem folder gets string like`":: ITEMIDLIST"`; see `Au.Types.Pidl`.

```csharp
public string GetFolderPath()
```

##### Returns

`string`

`null` if failed.