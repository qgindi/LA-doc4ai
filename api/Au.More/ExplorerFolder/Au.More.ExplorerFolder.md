# Class `Au.More.ExplorerFolder`

File Explorer folder window functions.

```csharp
public class ExplorerFolder
```

##### Remarks

Uses shell COM API, therefore must be used in STA threads. LibreAutomate script threads are STA.

##### Inheritance

`object` → `ExplorerFolder`

### Properties

`ComObject`, `Hwnd`, `HwndTab`

### Methods

`All`, `GetFolderPath`, `GetSelectedItems`, `NewTab`, `Of`, `Open`, `Select`, `SwitchToTab`, `ToString`