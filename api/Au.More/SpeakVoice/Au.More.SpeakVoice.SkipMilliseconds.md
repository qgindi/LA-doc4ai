# Method `Au.More.SpeakVoice.SkipMilliseconds`

Skips *count* milliseconds of speech.

```csharp
public void SkipMilliseconds(int count)
```

##### Parameters

- *count*  (`int`):
  Forward if positive, else backward.