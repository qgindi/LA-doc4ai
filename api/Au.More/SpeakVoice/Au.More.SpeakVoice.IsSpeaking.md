# Property `Au.More.SpeakVoice.IsSpeaking`

Returns `true` if currently is speaking. Returns `false` if finished or not started.

```csharp
public bool IsSpeaking { get; }
```

##### Property Value

`bool`