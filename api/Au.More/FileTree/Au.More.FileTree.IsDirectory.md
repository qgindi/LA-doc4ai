# Property `Au.More.FileTree.IsDirectory`

It is a directory. Or a NTFS link to a directory (see `Au.Types.FEFile.IsNtfsLink`).

```csharp
public bool IsDirectory { get; }
```

##### Property Value

`bool`