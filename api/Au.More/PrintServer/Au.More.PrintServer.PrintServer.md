# Constructor of `Au.More.PrintServer`

```
public PrintServer(bool isGlobal)
```

##### Parameters

- *isGlobal*  (`bool`):
    If `true`, will receive output from all processes that don't have local server.