# Method `Au.More.PrintServer.Start`

Starts server.

```
public bool Start()
```

##### Returns

`bool`

`false` if server already exists (if global - in any process).

##### Exceptions

- `Au.Types.AuException`:
    Failed.