# Property `Au.More.OcrTesseract.Language`

One or more of installed languages, like `"deu"` or `"eng+deu"`. If `null` (default), uses `"eng"`.

```
public string Language { get; set; }
```

##### Property Value

`string`