# Method `Au.More.ResourceUtil.HasResourcePrefix`

Returns `true` if string starts with `"resource:"` or `"resources/"`.

```
public static bool HasResourcePrefix(string s)
```

##### Parameters

- *s*  (`string`)

##### Returns

`bool`