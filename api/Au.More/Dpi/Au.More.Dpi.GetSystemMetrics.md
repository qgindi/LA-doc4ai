# Method `Au.More.Dpi.GetSystemMetrics`

Calls API `GetSystemMetricsForDpi` if available, else `GetSystemMetrics`.

```
public static int GetSystemMetrics(int nIndex, DpiOf dpiOf)
```

##### Parameters

- *nIndex*  (`int`)
- *dpiOf*  (`Au.Types.DpiOf`)

##### Returns

`int`