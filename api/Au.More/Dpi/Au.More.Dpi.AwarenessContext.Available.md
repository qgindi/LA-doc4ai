# Property `Au.More.Dpi.AwarenessContext.Available`

Returns `true` if thread DPI awareness contexts are available on this Windows version (Windows 10 1607 and later).

```
public static bool Available { get; }
```

##### Property Value

`bool`