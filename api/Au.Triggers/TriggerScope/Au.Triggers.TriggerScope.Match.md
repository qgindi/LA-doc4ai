# Method `Au.Triggers.TriggerScope.Match`

Returns `true` if the window matches this scope.

```
public bool Match(wnd w, WFCache cache)
```

##### Parameters

- *w*  (`Au.wnd`)
- *cache*  (`Au.Types.WFCache`)

##### Returns

`bool`