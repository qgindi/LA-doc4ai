# Property `Au.Triggers.TriggerFuncs.NextTriggerBeforeWindow`

Sets callback function for the next added trigger (except window triggers).
Called before evaluating the window scope (if any).

```csharp
public TFunc NextTriggerBeforeWindow { get; set; }
```

##### Property Value

`Au.Triggers.TFunc`