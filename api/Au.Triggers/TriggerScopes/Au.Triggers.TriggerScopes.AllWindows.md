# Method `Au.Triggers.TriggerScopes.AllWindows`

Sets scope "all windows" again. Hotkey, autotext and mouse triggers added afterwards will work with all windows.

```
public void AllWindows()
```

#### Remarks

Example in class help.