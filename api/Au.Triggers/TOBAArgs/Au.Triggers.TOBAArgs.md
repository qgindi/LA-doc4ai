# Struct `Au.Triggers.TOBAArgs`

Arguments for `Au.Triggers.TriggerOptions.BeforeAction` and `Au.Triggers.TriggerOptions.AfterAction`.

```
public struct TOBAArgs
```

### Properties

`ActionArgs`, `Exception`