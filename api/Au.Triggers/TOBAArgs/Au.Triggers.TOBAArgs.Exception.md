# Property `Au.Triggers.TOBAArgs.Exception`

If action ended with an exception, the exception. Else `null`.

```csharp
public readonly Exception Exception { get; }
```

##### Property Value

`Exception`