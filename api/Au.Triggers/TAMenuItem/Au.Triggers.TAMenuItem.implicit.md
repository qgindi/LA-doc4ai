# Operator `Au.Triggers.TAMenuItem.implicit operator`

Creates `TAMenuItem` with only `Au.Triggers.TAMenuItem.Text`.

```
public static implicit operator TAMenuItem(string text)
```

##### Parameters

- *text*  (`string`)

##### Returns

`Au.Triggers.TAMenuItem`