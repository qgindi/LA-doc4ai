# Property `Au.Types.TreeBase<T>.RootAncestor`

Returns the root ancestor node. Its `Au.Types.TreeBase<T>.Parent` is `null`. Returns this node if its `Parent` is `null`.

```
public T RootAncestor { get; }
```

##### Property Value

`T`