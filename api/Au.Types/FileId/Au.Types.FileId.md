# Struct `Au.Types.FileId`

See `Au.filesystem.more.getFileId`.

```
public record struct FileId : IEquatable<FileId>
```

### Constructors

`FileId(int, long)`

### Properties

`FileIndex`, `VolumeSerialNumber`