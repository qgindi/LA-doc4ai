# Property `Au.Types.DEventArgs.TimerTimeMS`

In a `Au.dialog.Timer` event handler - timer time in milliseconds. The handler can set `returnValue` = 1 to reset this.

```
public int TimerTimeMS { get; init; }
```

##### Property Value

`int`