# Property `Au.Types.SLTransaction.SqlOfDispose`

Gets or sets SQL to execute when disposing this variable if not called `Au.Types.SLTransaction.Commit` or `Au.Types.SLTransaction.Rollback`.
Initially = parameter`sqlOfDispose` of constructor.

```csharp
public string SqlOfDispose { get; set; }
```

##### Property Value

`string`