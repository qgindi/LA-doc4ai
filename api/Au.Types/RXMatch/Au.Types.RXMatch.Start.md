# Property `Au.Types.RXMatch.Start`

Gets start offset of the match in the subject string. The same as that of group 0 (`Au.Types.RXGroup.Start`).

```
public int Start { get; }
```

##### Property Value

`int`