# Property `Au.Types.RXMatch.IsPartial`

Returns `true` if this match is [partial](🔗).
Partial match is possible if used a`PARTIAL_` flag.

```csharp
public bool IsPartial { get; }
```

##### Property Value

`bool`