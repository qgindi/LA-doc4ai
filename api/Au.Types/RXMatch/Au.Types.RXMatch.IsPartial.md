# Property `Au.Types.RXMatch.IsPartial`

Returns `true` if this match is [partial](https://www.pcre.org/current/doc/html/pcre2partial.html). Partial match is possible if used a `PARTIAL_` flag.

```
public bool IsPartial { get; }
```

##### Property Value

`bool`