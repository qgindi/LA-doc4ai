# Property `Au.Types.FAttr.Unknown`

Exists but this process cannot access it and get attributes (error "access denied"). Then other `bool` properties return `false`.

```
public bool Unknown { get; }
```

##### Property Value

`bool`