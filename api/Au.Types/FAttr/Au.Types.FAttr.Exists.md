# Property `Au.Types.FAttr.Exists`

Exists and is accessible (`Au.Types.FAttr.Unknown` `false`). See also `Au.Types.FAttr.File`, `Au.Types.FAttr.Directory`.

```
public bool Exists { get; }
```

##### Property Value

`bool`