# Property `Au.Types.WBGridLength.Row`

Creates row definition object from assigned height or/and min/max height values. Or just returns the assigned or previously created object.

```
public RowDefinition Row { get; }
```

##### Property Value

`RowDefinition`