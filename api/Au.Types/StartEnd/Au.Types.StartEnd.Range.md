# Property `Au.Types.StartEnd.Range`

Converts this to `Au.Types.StartEnd.Range`. Can be used to get substring, like `s[x.Range]` instead of `s[x.start..x.end]`.

```
public Range Range { get; }
```

##### Property Value

`Range`