# Field `Au.Types.ROptions.Verb`

File's right-click menu command, also known as verb. For example `"edit"`, `"print"`, `"properties"`.
The default verb is bold in the menu.
Not all menu items will work. Some may have different name than in the menu.

```csharp
public string Verb
```