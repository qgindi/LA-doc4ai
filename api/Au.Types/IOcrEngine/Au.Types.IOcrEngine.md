# Interface `Au.Types.IOcrEngine`

Used by `Au.ocr` and `Au.ocrFinder`.

```csharp
public interface IOcrEngine
```

### Properties

`DpiScale`

### Methods

`GetBitmapData`, `GetBitmapPngFileData`, `PolyToRect`, `PrepareBitmap`, `Recognize`