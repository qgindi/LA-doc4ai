# Property `Au.Types.JSettings.NoAutoSaveTimer`

Don't call `Au.Types.JSettings.SaveIfNeed` every 2 s. Default `false`.

```
protected bool NoAutoSaveTimer { get; set; }
```

##### Property Value

`bool`