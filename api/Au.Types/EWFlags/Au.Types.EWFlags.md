# Enum `Au.Types.EWFlags`

Flags for `Au.elm.fromWindow`.

```
[Flags]
public enum EWFlags
```

### Fields

| Name | Description |
| --- | --- |
| NoThrow | Don't throw exception when fails. Then returns `null`. |
| NotInProc | Don't load dll into the target process. More info: `Au.Types.EFFlags.NotInProc`. |