# Method `Au.Types.ExtWpf.InsertRow`

Inserts row and adjusts row indices of children that are in other rows.

```
public static void InsertRow(this Grid t, int index, RowDefinition d)
```

##### Parameters

- *t*  (`Grid`)
- *index*  (`int`)
- *d*  (`RowDefinition`)