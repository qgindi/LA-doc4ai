# Property `Au.Types.FileProperties.IsNtfsLink`

It is a NTFS link, such as symbolic link or mount point. Don't confuse with shell links (shortcuts).

```
public bool IsNtfsLink { readonly get; set; }
```

##### Property Value

`bool`