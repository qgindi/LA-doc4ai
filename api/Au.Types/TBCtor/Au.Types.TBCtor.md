# Enum `Au.Types.TBCtor`

Flags for `Au.toolbar` constructor.

```csharp
[Flags]
public enum TBCtor
```

## Fields

### `DontSaveSettings`

Don't load and save settings. No file will be created or opened.

### `ResetSettings`

Don't load saved settings. Delete the settings file of the toolbar, if exists.