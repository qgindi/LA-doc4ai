# Property `Au.Types.CREATESTRUCT.ClassName`

If `lpszClass` is atom, returns string with `#` prefix and atom value, like `"#32770"`.

```
public ReadOnlySpan<char> ClassName { get; }
```

##### Property Value

`ReadOnlySpan<char>`