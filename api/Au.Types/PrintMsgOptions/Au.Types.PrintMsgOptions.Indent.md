# Property `Au.Types.PrintMsgOptions.Indent`

Prepend one or more tabs if the caller function (usually a window procedure) is called recursively. Default `true`.

```
public bool Indent { get; set; }
```

##### Property Value

`bool`