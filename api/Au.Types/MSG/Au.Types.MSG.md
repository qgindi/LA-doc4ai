# Struct `Au.Types.MSG`

API `MSG`

```
public struct MSG
```

### Fields

`hwnd`, `lParam`, `message`, `pt`, `time`, `wParam`

### Methods

`ToString`

### Operators

`implicit operator MSG(in Message)`, `implicit operator MSG(in MSG)`