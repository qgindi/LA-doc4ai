# Struct `Au.Types.TBOffsets`

Used with `Au.toolbar.Offsets`.

```
public record struct TBOffsets : IEquatable<TBOffsets>
```

### Constructors

`TBOffsets(double, double, double, double)`

### Properties

`Bottom`, `Left`, `Right`, `Top`