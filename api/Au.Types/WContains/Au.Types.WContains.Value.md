# Property `Au.Types.WContains.Value`

Gets object stored in this variable. Can be `null`, `Au.wndChildFinder`, `Au.elmFinder`, `Au.uiimageFinder` or `Au.ocrFinder`.

```
public object Value { get; }
```

##### Property Value

`object`