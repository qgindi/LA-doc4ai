# Property `Au.Types.ClipFormats.Html`

The HTML format. Registered, name `"HTML Format"`. Used by `Au.clipboardData` add/get HTML functions.

```csharp
public static int Html { get; }
```

##### Property Value

`int`