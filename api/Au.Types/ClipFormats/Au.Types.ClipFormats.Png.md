# Property `Au.Types.ClipFormats.Png`

The PNG format. Registered, name `"PNG"`. Used by `Au.clipboardData` add/get image functions.

```
public static int Png { get; }
```

##### Property Value

`int`