# Property `Au.Types.HSMessage.Content`

Raw content. For example POST data as UTF-8 text or binary.

```
public byte[] Content { get; }
```

##### Property Value

`byte[]`

`null` if the message is without content.