# Property `Au.Types.HSMessage.RawTarget`

Target, like `"/file.html"` or `"/file.html?a=1&b=2"` or `"/"`. May be URL-encoded.

```
public string RawTarget { get; }
```

##### Property Value

`string`