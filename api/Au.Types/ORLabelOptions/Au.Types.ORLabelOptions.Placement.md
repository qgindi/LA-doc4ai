# Property `Au.Types.ORLabelOptions.Placement`

Label placement relative to rectangle: `'L'` (left), `'R'` (right), `'T'` (top), `'B'` (bottom) or `'I'` (inside). Default: `'L'`.

```csharp
public char Placement { get; init; }
```

##### Property Value

`char`