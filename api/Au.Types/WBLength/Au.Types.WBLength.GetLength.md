# Method `Au.Types.WBLength.GetLength`

Gets the width or height value. Returns `false` if not set.

```
public bool GetLength(out double value)
```

##### Parameters

- *value*  (`double`)

##### Returns

`bool`