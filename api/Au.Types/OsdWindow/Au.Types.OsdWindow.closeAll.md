# Method `Au.Types.OsdWindow.closeAll`

Closes all OSD windows of this process.

```csharp
public static void closeAll(string name = null)
```

##### Parameters

- *name*  (`string`):
  If not `null`, closes only OSD windows whose `Au.Types.OsdWindow.Name` matches this [wildcard expression](🔗).