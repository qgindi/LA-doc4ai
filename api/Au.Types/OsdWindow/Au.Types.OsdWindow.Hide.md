# Method `Au.Types.OsdWindow.Hide`

Hides the OSD window. Does not destroy; use `Au.Types.OsdWindow.Close` or `Au.Types.OsdWindow.Dispose`for it.
Does nothing if not created or not visible.

```csharp
public virtual void Hide()
```