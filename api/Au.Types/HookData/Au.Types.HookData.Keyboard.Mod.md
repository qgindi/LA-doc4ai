# Property `Au.Types.HookData.Keyboard.Mod`

If the key is a modifier key (`Shift`, `Ctrl`, `Alt`, `Win`), returns the modifier flag. Else returns 0.

```
public KMod Mod { get; }
```

##### Property Value

`Au.Types.KMod`