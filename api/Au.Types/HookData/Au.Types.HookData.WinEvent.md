# Struct `Au.Types.HookData.WinEvent`

Hook data for the hook procedure set by `Au.More.WinEventHook`. More info: API `WinEventProc`.

```
public struct HookData.WinEvent
```

### Fields

`event_`, `hook`, `idChild`, `idObject`, `thread`, `time`, `w`

### Methods

`GetElm`