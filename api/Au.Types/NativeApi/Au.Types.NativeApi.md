# Class `Au.Types.NativeApi`

If a class is derived from this class, editor adds undeclared Windows API to its completion list.

```
public abstract class NativeApi
```

##### Inheritance

`object` → `NativeApi`