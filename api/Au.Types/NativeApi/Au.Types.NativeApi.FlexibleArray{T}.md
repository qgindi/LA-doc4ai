# Struct `Au.Types.NativeApi.FlexibleArray<T>`

Can be used in structures as flexible array member (the last field, defined like `Type[1] name;` in C).

```
public struct NativeApi.FlexibleArray<T> where T : unmanaged
```

##### Type Parameters

| Name | Description |
| --- | --- |
| T |  |

### Properties

`this[int]`

### Methods

`AsSpan`