# Method `Au.Types.RECT.Move`

Moves this rectangle so that `left`=*x* and `right`=*y*. Does not change `Au.Types.RECT.Width` and `Au.Types.RECT.Height`.

```
public void Move(int x, int y)
```

##### Parameters

- *x*  (`int`)
- *y*  (`int`)