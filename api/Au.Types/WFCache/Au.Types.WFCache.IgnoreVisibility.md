# Property `Au.Types.WFCache.IgnoreVisibility`

Match invisible and cloaked windows too, even if the flags are not set (see `Au.Types.WFlags`).

```
public bool IgnoreVisibility { get; set; }
```

##### Property Value

`bool`