# Field `Au.Types.OKeyHookData.optk`

Options used by the "send keys or text" function. The callback function can modify them, except `Hook`, `NoModOff`, `NoCapsOff`, `NoBlockInput`.

```
public readonly OKey optk
```