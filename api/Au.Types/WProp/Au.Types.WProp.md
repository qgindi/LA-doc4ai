# Struct `Au.Types.WProp`

Sets, gets, removes and lists window properties using API `SetProp` and co.

```
public struct WProp
```

### Properties

`this[string]`, `this[ushort]`

### Methods

`GetList`, `Remove`, `Remove`, `Set`, `Set`, `ToString`