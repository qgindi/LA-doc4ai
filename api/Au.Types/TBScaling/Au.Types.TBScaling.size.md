# Field `Au.Types.TBScaling.size`

Scale toolbar size and related properties. If default (`null`), scales size, except of empty toolbars created by `Au.toolbar.AutoHideScreenEdge`.

```
public bool? size
```