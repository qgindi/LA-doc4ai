# Field `Au.Types.TBScaling.offsets`

Scale toolbar offsets. See `Au.toolbar.Offsets`. If default (`null`), scales offsets, except when anchor is screen (not window etc).

```
public bool? offsets
```