# Property `Au.Types.Seconds.DoEvents`

Use `Au.wait.doEvents` instead of `Au.wait.ms`.
If`null`, will be used `false`.

```csharp
public bool? DoEvents { get; set; }
```

##### Property Value

`bool?`