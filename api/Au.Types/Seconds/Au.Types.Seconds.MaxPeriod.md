# Property `Au.Types.Seconds.MaxPeriod`

Sets `Au.More.WaitLoop.MaxPeriod`.
If`null` (default), it will use `Period*50`.

```csharp
public int? MaxPeriod { get; set; }
```

##### Property Value

`int?`