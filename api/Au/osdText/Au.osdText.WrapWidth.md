# Property `Au.osdText.WrapWidth`

Maximal text width.
Default: 0 - no limit (depends on screen width etc).

```csharp
public int WrapWidth { get; set; }
```

##### Property Value

`int`

#### Remarks

This property cannot be changed after creating OSD window.