# Property `Au.uacInfo.isUacDisabled`

Returns `true` if [UAC](../articles/UAC.html) is disabled (turned off) completely (not just disabled UAC consent screen/dialog).

```
public static bool isUacDisabled { get; }
```

##### Property Value

`bool`