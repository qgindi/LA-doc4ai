# Property `Au.dialog.options.defaultTitle`

Default title bar text. Default value - `Au.script.name`. In exe it is exe file name like `"Example.exe"`.

```
public static string defaultTitle { get; set; }
```

##### Property Value

`string`