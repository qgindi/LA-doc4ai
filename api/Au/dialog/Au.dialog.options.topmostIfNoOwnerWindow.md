# Property `Au.dialog.options.topmostIfNoOwnerWindow`

If there is no owner window, let the dialog be always on top of most other windows. Default `true`.

```
public static bool topmostIfNoOwnerWindow { get; set; }
```

##### Property Value

`bool`

### See Also

`Au.Types.DFlags`