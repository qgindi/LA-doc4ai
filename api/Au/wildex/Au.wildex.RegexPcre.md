# Property `Au.wildex.RegexPcre`

Returns the `Au.regexp` object created from regular expression string. `null` if `Au.wildex.TextType` is not `RegexPcre` (no option `r`).

```
public regexp RegexPcre { get; }
```

##### Property Value

`Au.regexp`