# Property `Au.wildex.MultiArray`

Array of `Au.wildex` variables, one for each part in multi-part text. `null` if `Au.wildex.TextType` is not `Multi` (no option `m`).

```
public wildex[] MultiArray { get; }
```

##### Property Value

`Au.wildex[]`