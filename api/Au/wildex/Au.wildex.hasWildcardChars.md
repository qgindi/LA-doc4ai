# Method `Au.wildex.hasWildcardChars`

Returns `true` if string contains wildcard characters: `'*'`, `'?'`.

```
public static bool hasWildcardChars(ReadOnlySpan<char> s)
```

##### Parameters

- *s*  (`ReadOnlySpan<char>`):
    Can be `null`.

##### Returns

`bool`