# Property `Au.miscInfo.isChildSession`

Returns `true` if this process is running in a child session (aka Picture-in-Picture).

```csharp
public static bool isChildSession { get; }
```

##### Property Value

`bool`