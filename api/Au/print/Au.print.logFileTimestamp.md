# Property `Au.print.logFileTimestamp`

If `true`, will add current local time when using log file (see `Au.print.logFile`).

```
public static bool logFileTimestamp { get; set; }
```

##### Property Value

`bool`