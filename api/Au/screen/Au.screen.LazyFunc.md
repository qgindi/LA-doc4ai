# Property `Au.screen.LazyFunc`

Gets the callback function that returns `Au.screen` when need. Returns `null` if it wasn't set.

```
public Func<screen> LazyFunc { get; }
```

##### Property Value

`Func<Au.screen>`