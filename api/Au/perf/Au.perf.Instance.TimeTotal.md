# Property `Au.perf.Instance.TimeTotal`

Gets the number of microseconds between `Au.perf.Instance.First` and the last `Au.perf.Instance.Next`.

```csharp
public long TimeTotal { get; }
```

##### Property Value

`long`