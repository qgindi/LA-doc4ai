# Method `Au.perf.local`

Creates and returns new `Au.perf.Instance` variable and calls its `Au.perf.Instance.First`.

```
public static perf.Instance local()
```

##### Returns

`Au.perf`.`Au.perf.Instance`