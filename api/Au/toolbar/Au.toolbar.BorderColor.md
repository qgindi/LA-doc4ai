# Property `Au.toolbar.BorderColor`

Border color when `Au.toolbar.Border` is `Au.Types.TBBorder.Width1` ... `Au.Types.TBBorder.Width4`. If `default`, uses system color.

```
public ColorInt BorderColor { get; set; }
```

##### Property Value

`Au.Types.ColorInt`