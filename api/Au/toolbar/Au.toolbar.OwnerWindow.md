# Property `Au.toolbar.OwnerWindow`

Returns the owner top-level window. Returns `default(wnd)` if the toolbar is not owned. See `Au.toolbar.IsOwned`.

```
public wnd OwnerWindow { get; }
```

##### Property Value

`Au.wnd`