# Property `Au.timer.IsRunning`

`true`if the timer is started and not stopped.
Note: single-period timer is automatically stopped before calling the callback function.

```csharp
public bool IsRunning { get; }
```

##### Property Value

`bool`