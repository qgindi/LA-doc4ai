# Operator `Au.wnd.implicit operator`

Converts from a special handle value.

```
public static implicit operator wnd(SpecHWND hwnd)
```

##### Parameters

- *hwnd*  (`Au.Types.SpecHWND`):
    See API `SetWindowPos`.

##### Returns

`Au.wnd`