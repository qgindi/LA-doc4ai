# Property `Au.wnd.ClassName`

Gets window class name.

```
public string ClassName { get; }
```

##### Property Value

`string`

`null` if failed, eg if the window is closed. Supports `Au.lastError`.