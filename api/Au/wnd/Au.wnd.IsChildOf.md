# Method `Au.wnd.IsChildOf`

Returns `true` if this is a child or descendant of window *w*.

```
public bool IsChildOf(wnd w)
```

##### Parameters

- *w*  (`Au.wnd`)

##### Returns

`bool`

#### Remarks

Calls API `IsChild`. Supports `Au.lastError`.