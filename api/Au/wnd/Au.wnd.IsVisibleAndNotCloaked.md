# Property `Au.wnd.IsVisibleAndNotCloaked`

Returns `true` if `Au.wnd.IsVisible` returns `true` and `Au.wnd.IsCloaked` returns `false`.

```
public bool IsVisibleAndNotCloaked { get; }
```

##### Property Value

`bool`