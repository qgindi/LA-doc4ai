# Method `Au.wnd.thisThread.isFocused`

Returns `true` if *w* is the focused control or window of this thread.

```
public static bool isFocused(wnd w)
```

##### Parameters

- *w*  (`Au.wnd`)

##### Returns

`bool`

#### Remarks

Calls API `GetFocus`.