# Property `Au.wnd.IsToolWindow`

Returns `true` if has `Au.Types.WSE.TOOLWINDOW` style.

```csharp
public bool IsToolWindow { get; }
```

##### Property Value

`bool`

#### Remarks

Supports `Au.lastError`.