# Method `Au.wnd.GetRectNotMinMax`

Gets rectangle of normal (restored) window even if currently it is minimized or maximized.

```
public bool GetRectNotMinMax(out RECT r)
```

##### Parameters

- *r*  (`Au.Types.RECT`)

##### Returns

`bool`

#### Remarks

Supports `Au.lastError`.