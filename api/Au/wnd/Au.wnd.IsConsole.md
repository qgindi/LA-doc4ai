# Property `Au.wnd.IsConsole`

Returns `true` if this is a console window (class name `"ConsoleWindowClass"`).

```
public bool IsConsole { get; }
```

##### Property Value

`bool`

#### Remarks

Supports `Au.lastError`.