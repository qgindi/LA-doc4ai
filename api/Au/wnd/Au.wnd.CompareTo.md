# Method `Au.wnd.CompareTo`

Implements `System.IComparable<T>`. It allows to sort a collection.

```
public int CompareTo(wnd other)
```

##### Parameters

- *other*  (`Au.wnd`)

##### Returns

`int`

##### Implements

`IComparable<T>.CompareTo(T)`