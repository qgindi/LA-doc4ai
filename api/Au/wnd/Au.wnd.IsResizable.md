# Property `Au.wnd.IsResizable`

Returns `true` if has `Au.Types.WS.THICKFRAME` style.

```csharp
public bool IsResizable { get; }
```

##### Property Value

`bool`

#### Remarks

Supports `Au.lastError`.