# Method `Au.wnd.ZorderIsAbove`

Returns `true` if this window is above window *w* in the Z order.

```csharp
public bool ZorderIsAbove(wnd w)
```

##### Parameters

- *w*  (`Au.wnd`)

##### Returns

`bool`