# Constructor of `Au.icon`

Sets native icon handle. The icon will be destroyed when disposing this variable or when converting to object of other type.

```
public icon(nint hicon)
```

##### Parameters

- *hicon*  (`nint`)