# Property `Au.osVersion.isArm64Process`

`true` if this process is ARM64.

```
public static bool isArm64Process { get; }
```

##### Property Value

`bool`

`RuntimeInformation.ProcessArchitecture == Architecture.Arm64`