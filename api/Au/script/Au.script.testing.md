# Property `Au.script.testing`

Returns `true` if this script task was started from editor with the **Run** button or menu command. Always `false` if role `editorExtension`.

```
public static bool testing { get; }
```

##### Property Value

`bool`