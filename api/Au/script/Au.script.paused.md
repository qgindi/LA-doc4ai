# Property `Au.script.paused`

If `true`, next call to `Au.script.pause` will wait until `false`, or already is waiting.

```csharp
public static bool paused { get; set; }
```

##### Property Value

`bool`