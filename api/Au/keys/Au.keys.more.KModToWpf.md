# Method `Au.keys.more.KModToWpf`

Converts modifier key flags from `KMod` to WPF `ModifierKeys`.

```
public static ModifierKeys KModToWpf(KMod mod)
```

##### Parameters

- *mod*  (`Au.Types.KMod`)

##### Returns

`ModifierKeys`