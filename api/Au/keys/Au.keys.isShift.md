# Property `Au.keys.isShift`

Returns `true` if the `Shift` key is pressed. Calls `Au.keys.isPressed`. In UI code use `Au.keys.gui` instead.

```
public static bool isShift { get; }
```

##### Property Value

`bool`