# Property `Au.keys.gui.isScrollLock`

Returns `true` if the `ScrollLock` key is toggled.

```
public static bool isScrollLock { get; }
```

##### Property Value

`bool`

#### Remarks

The same as `Au.keys.isScrollLock`.