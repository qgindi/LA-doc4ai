# Property `Au.keys.isCtrl`

Returns `true` if the `Ctrl` key is pressed. Calls `Au.keys.isPressed`. In UI code use `Au.keys.gui` instead.

```
public static bool isCtrl { get; }
```

##### Property Value

`bool`