# Property `Au.keys.isAlt`

Returns `true` if the `Alt` key is pressed. Calls `Au.keys.isPressed`. In UI code use `Au.keys.gui` instead.

```
public static bool isAlt { get; }
```

##### Property Value

`bool`