# Property `Au.keys.gui.isNumLock`

Returns `true` if the `NumLock` key is toggled.

```
public static bool isNumLock { get; }
```

##### Property Value

`bool`

#### Remarks

The same as `Au.keys.isNumLock`.