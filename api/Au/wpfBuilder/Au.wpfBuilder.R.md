# Property `Au.wpfBuilder.R`

Starts new auto-sized row in current grid. The same as `Row(0)`. See `Au.wpfBuilder.Row`.

```
public wpfBuilder R { get; }
```

##### Exceptions

- `InvalidOperationException`:
    In non-grid panel.

##### Property Value

`Au.wpfBuilder`