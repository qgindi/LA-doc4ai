# Method `Au.shortcutFile.Save`

Saves to the shortcut file (`.lnk`).

```
public void Save()
```

##### Exceptions

- `Au.Types.AuException`:
    Failed to save.

#### Remarks

Creates parent folder if need.