# Property `Au.consoleProcess.InputEncoding`

Input text encoding for `Au.consoleProcess.Write`. If `null` (default), will use `Au.consoleProcess.Encoding`.

```
public Encoding InputEncoding { get; set; }
```

##### Property Value

`Encoding`