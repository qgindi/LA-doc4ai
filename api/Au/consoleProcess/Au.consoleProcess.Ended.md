# Property `Au.consoleProcess.Ended`

Returns `true` if a `ReadX` function detected that the console output stream is closed. The process is ended or ending.

```
public bool Ended { get; }
```

##### Property Value

`bool`