# Property `Au.consoleProcess.TerminateFinally`

If the console process is still running when this variable is dying, terminate it. Default `true`.

```
public bool TerminateFinally { get; set; }
```

##### Property Value

`bool`