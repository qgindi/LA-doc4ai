# Property `Au.elm.IsPassword`

Calls `Au.elm.State` and returns `true` if has state `PROTECTED`.

```
public bool IsPassword { get; }
```

##### Property Value

`bool`

#### Remarks

This state is used for password fields.