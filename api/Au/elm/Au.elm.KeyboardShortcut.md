# Property `Au.elm.KeyboardShortcut`

Gets keyboard shortcut.

```csharp
public string KeyboardShortcut { get; }
```

##### Property Value

`string`

`""` if this property is unavailable or if failed. Supports `Au.lastError`.