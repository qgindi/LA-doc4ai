# Method `Au.elm.focused`

Gets the keyboard-focused UI element.

```csharp
public static elm focused(EFocusedFlags flags = 0)
```

##### Parameters

- *flags*  (`Au.Types.EFocusedFlags`):
  Enum: NotInProc, UIA.

##### Returns

`Au.elm`

`null` if failed.